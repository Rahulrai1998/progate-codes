const cafe = {
  name: "Progate Cafe",
  businessHours: {
    // Assign the provided object to businessHours
    opening:"10:00(AM)",closing:"8:00(PM)"
    
  },
};
console.log(`Name:${cafe.name}`);
console.log(`Hours: From ${cafe. businessHours.opening} to ${cafe. businessHours.closing}`);// Print "Name: ____"


// Print "Hours: From ____ to ____
